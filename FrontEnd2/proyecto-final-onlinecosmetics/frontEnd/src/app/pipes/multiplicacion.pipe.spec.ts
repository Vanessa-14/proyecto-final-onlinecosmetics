import { MultiplicacionPipe } from './multiplicacion.pipe';

describe('MultiplicacionPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplicacionPipe();
    expect(pipe).toBeTruthy();
  });
});
